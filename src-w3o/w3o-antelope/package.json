{
  "name": "w3o-antelope",
  "version": "0.1.0",
  "description": "Web3 Octopus Antelope Support",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "scripts": {
    "copy-version": "node scripts/copy_version.js",
    "build": "npm run copy-version; tsc",
    "test": "jest",
    "prepublish": "npm run build",
    "dev": "npm run copy-version; tsc -w",
    "publish-on": "npm run build && npm publish --access public",
    "src": "node scripts/concatenate_src.js"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.5",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "rxjs": ">=6.0.0"
  },
  "dependencies": {
    "rxjs": ">=6.0.0"
  }
}
  