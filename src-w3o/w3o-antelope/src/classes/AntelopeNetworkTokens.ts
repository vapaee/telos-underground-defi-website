import {
    Logger,
    LoggerContext,
    W3oContractManager,
    W3oNetwork,
    W3oNetworkSettings,
    W3oToken,
    W3oInstance,
    W3oModule,
} from "@vapaee/w3o-core";

import { Observable } from "rxjs";
import { AntelopeContractManager } from "./AntelopeContractManager";
import { W3oAntelopeNetworkSettings } from "../types";

const logger = new Logger('AntelopeNetworkTokens');
export class AntelopeNetworkTokens {


}
